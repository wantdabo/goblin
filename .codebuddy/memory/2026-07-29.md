# 2026-07-29

## 全部 Behavior 提升至 Sa 级

将 7 个 per-actor Behavior 全部迁移到 Sa 级（System Actor），每个 Behavior 类型全局仅一个实例。

### 新建文件（Behaviors/Sa/）
- `Tag.cs` — Tag（标签系统）
- `Movement.cs` — Movement（运动系统）
- `StateMachine.cs` — StateMachine（状态机）
- `Gamepad.cs` — Gamepad（输入中枢）
- `Facade.cs` — Facade（外观管理）
- `HUD.cs` — HUD（HUD 同步）
- `SkillLauncher.cs` — SkillLauncher（技能释放）

### 删除文件（Behaviors/）
- 同上 7 个旧 per-actor Behavior 文件

### 修改文件
- `StateMachineInfo.cs` — 新增 `stateduration(FP)`、`timerfallback(byte)` 字段
- `Stage.cs` — 新增 7 个快捷属性；`Behaviors()` 添加 7 个 Sa Behavior；`AddActor()` 改为 AddBehaviorInfo；简化 Restore
- `Prefab.cs`、`HeroPrefab.cs`、`EnemyPrefab.cs` — 改为 AddBehaviorInfo 模式
- `FlowPrefab.cs` — 补充 using System.Collections.Generic
- 全部 Executor/Checker 改用 stage shortcut 调用
- `TICK_DEFINE.cs` using 更新为 Behaviors.Sa

### 编译结果
- 0 个错误，所有警告来自第三方库和现有代码

## Behaviors 目录扁平化

将 `Behaviors/Sa/` 所有 22 个 .cs 文件移至 `Behaviors/` 根目录，删除 `Sa` 子目录。
- 命名空间：`Goblin.Gameplay.Logic.Behaviors.Sa` → `Goblin.Gameplay.Logic.Behaviors`
- 所有 `using Behaviors.Sa` 更新为 `using Behaviors`
- `Stage.cs` 中别名 using（`SaEvent`、`Config`、`Random`）同步更新
- `Tests/Goblin.Logic.Standalone.csproj` 中 ProjectorSystem.cs 路径更新
- BehaviorInfo 保持 `BehaviorInfos/` 和 `BehaviorInfos/Sa/` 分目录不变
- 编译通过，0 错误
